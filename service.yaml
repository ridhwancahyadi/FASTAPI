apiVersion: v1
kind: Service
metadata:
  name: finance-tracker-service
spec:
  selector:
    app: finance-tracker
  type: NodePort
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30080